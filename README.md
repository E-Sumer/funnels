
  # Update Prototype with Design System

  This is a code bundle for Update Prototype with Design System. The original project is available at https://www.figma.com/design/cRyLGKTw3rfZ2HDT8N7dry/Update-Prototype-with-Design-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  